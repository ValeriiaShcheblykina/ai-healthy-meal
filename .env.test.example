# E2E Test Environment Variables Template
# Copy this file to .env.test and fill in your values

# Supabase Connection (Required for E2E tests)
# Get these from: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Test User Credentials
# These credentials should match a user that exists in your Supabase database
E2E_USERNAME=test+e2e@example.com
E2E_PASSWORD=YourPasswordHere

# Base URL for testing (optional)
BASE_URL=http://localhost:3000

# Instructions:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Fill in your Supabase URL and keys from Supabase Dashboard
# 3. Create a test user in Supabase and set E2E_USERNAME and E2E_PASSWORD
# 4. Run your tests: npm run test:e2e
#
# IMPORTANT SECURITY NOTES:
# - NEVER commit .env.test to version control
# - The SERVICE_ROLE_KEY is required for test cleanup (deleting test users)
# - Keep your service role key secure - it has admin privileges
